# Test des entrées et sorties
# Sortie attendue :
# Hello World !
# var = 12
# Saisissez une valeur : [42]
# var = 42

procedure principale()
    ecrire("Hello World !");
    var = 12;
    ecrire("var = ", var);

    ecrire("Saisissez une valeur : ");
    lire(var);
    ecrire("var = ", var);
finproc